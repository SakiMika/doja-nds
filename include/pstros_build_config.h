#ifndef PSTROS_BUILD_CONFIG_H
#define PSTROS_BUILD_CONFIG_H

/* Placeholder config header for local project includes.
   Keep this folder because the template-style Makefile expects include/. */

#endif
